# Bulk data / caches / generated trees — machine-local, never versioned.
# Anchored to the repo root so they do not match paths inside results/.
/runs/
/data/
/scratch/
/.cache/
/.conda/
/.Trash-1000/
/ncbi_cache/
/COTAN/
/logs/
/.vscode/

# Nextflow intermediates and bulk sequence/reference data
/.nextflow/
/work/
/output/
*.fastq
*.fastq.gz
*.fq
*.fq.gz
*.bam
*.bai
*.sam
*.sra

# Third-party papers — cited, never redistributed.
COTAN_*.pdf

.DS_Store
/*.log
