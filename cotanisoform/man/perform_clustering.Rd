% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/33_clustering.R
\name{perform_clustering}
\alias{perform_clustering}
\title{Perform Uniform Clustering and Merging}
\usage{
perform_clustering(
  cotan_obj,
  cl_name = "merged",
  gdi_threshold = NaN,
  cores = 1L,
  optimize_for_speed = TRUE,
  device_str = "cuda",
  save_obj = FALSE,
  output_dir = NULL,
  file_name = "cotan_clustered.rds",
  checker = NULL,
  initial_resolution = 0.8,
  max_iterations = 25L,
  use_dea = TRUE,
  distance = NULL,
  use_coex_eigen = FALSE,
  data_method = "",
  genes_sel = "HVG_Seurat",
  num_genes = 2000L,
  num_reduced_comp = 25L,
  hclust_method = "ward.D2",
  initial_clusters = NULL,
  minimum_ut_cluster_size = 50L,
  initial_iteration = 1L,
  clusters = NULL,
  checkers = NULL,
  batch_size = 0L,
  all_check_results = data.frame()
)
}
\arguments{
\item{cotan_obj}{A \code{COTAN} object.}

\item{gdi_threshold}{Numeric. Threshold for GDI clustering.}

\item{cores}{Integer. Number of cores to use. Default is 1L.}

\item{optimize_for_speed}{Boolean. Try using the torch library to run matrix calculations. Default is TRUE.}

\item{device_str}{Character string. Device to use for calculations ("cpu", "cuda"). Default is "cuda".}

\item{save_obj}{Boolean. Save intermediate COTAN objects and plots to disk. Default is FALSE.}

\item{output_dir}{Character. Optional directory to save intermediate outputs and the final output object.
Default is \code{NULL}.}

\item{file_name}{Character. Optional file name for saving the final output object.
Default is \code{"cotan_clustered.rds"}.}

\item{checker}{A \code{BaseUniformityCheck} object. Default is \code{NULL}.}

\item{initial_resolution}{Numeric. Default is 0.8.}

\item{max_iterations}{Integer. Default is 25L.}

\item{use_dea}{Boolean. Default is TRUE.}

\item{distance}{Character. Default is \code{NULL}.}

\item{use_coex_eigen}{Boolean. Default is FALSE.}

\item{data_method}{Character. Default is \code{""}.}

\item{genes_sel}{Character. Default is \code{"HVG_Seurat"}.}

\item{num_genes}{Integer. Default is 2000L.}

\item{num_reduced_comp}{Integer. Default is 25L.}

\item{hclust_method}{Character. Default is \code{"ward.D2"}.}

\item{initial_clusters}{Character vector. Default is \code{NULL}.}

\item{minimum_ut_cluster_size}{Integer. Default is 50L.}

\item{initial_iteration}{Integer. Default is 1L.}

\item{clusters}{A clusterization to merge. Default is \code{NULL}.}

\item{checkers}{A list of uniformity checkers. Default is \code{NULL}.}

\item{batch_size}{Integer. Default is 0L.}

\item{all_check_results}{A data frame. Default is \code{data.frame()}.}
}
\value{
A clustered \code{COTAN} object with "split" and "merged" clusterizations added.
}
\description{
Performs GDI-based uniform transcript clustering to find homogenous cell populations,
then merges highly similar clusters to prevent over-clustering.
Automatically extracts the resulting clusters and co-expression matrices and stores
them back into the COTAN object.
}
