% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/20_seurat.R
\name{filter_iterative_sparsity}
\alias{filter_iterative_sparsity}
\title{Iteratively Filter Sparse Matrix in Seurat Object}
\usage{
filter_iterative_sparsity(
  seurat_obj,
  cells_cutoff = 0.003,
  genes_cutoff = 0.002,
  loss_threshold = 0.5,
  output_dir = NULL,
  file_name = "seurat_sparsity_filtered.rds"
)
}
\arguments{
\item{seurat_obj}{A \code{Seurat} object.}

\item{cells_cutoff}{Numeric. Fraction of cells a feature must be expressed in. Default is 0.003.}

\item{genes_cutoff}{Numeric. Fraction of features a cell must express. Default is 0.002.}

\item{loss_threshold}{Numeric. Threshold (0-1) to trigger a warning for data loss. Default is 0.5 (50\%).}

\item{output_dir}{Character. Optional directory to save the output object as an RDS file. Default is \code{NULL}.}

\item{file_name}{Character. Optional file name for saving the output object.
Default is \code{"seurat_sparsity_filtered.rds"}.}
}
\value{
A new, subsetted \code{Seurat} object with the converged dimensions.
}
\description{
Extracts the raw sparse matrix from a Seurat object and iteratively applies
cutoffs for minimum expressing cells per gene and minimum expressed genes per cell.
It loops on the dgCMatrix until convergence (zero features or cells dropped).
}
