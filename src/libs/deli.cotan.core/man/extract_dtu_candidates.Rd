% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/500_extract_dtu_candidates.R
\name{extract_dtu_candidates}
\alias{extract_dtu_candidates}
\title{Extract Differential Transcript Usage (DTU) Candidates}
\usage{
extract_dtu_candidates(
  cotan_object,
  clusterization_name = "Known_Cell_Types",
  p_value_threshold = 0.05,
  coex_threshold = -0.1,
  min_dea_contrast = 1,
  output_directory = NULL,
  file_name = "dtu_candidates.csv",
  cores = 1L,
  chunk_size = 1024L
)
}
\arguments{
\item{cotan_object}{A \code{COTAN} object with calculated COEX and a valid clusterization.}

\item{clusterization_name}{Character. The name of the clusterization to use for DEA. Default is \code{"Known_Cell_Types"}.}

\item{p_value_threshold}{Numeric. Maximum p-value to consider a COEX score significant. Default is \code{0.05}.}

\item{coex_threshold}{Numeric. Maximum COEX score (must be negative) to define mutual exclusivity. Default is \code{-0.1}.}

\item{min_dea_contrast}{Numeric. Minimum absolute difference in enrichment scores to define a valid switch.
Default is \code{1.0}.}

\item{output_directory}{Character. Directory to save the output CSV. Default is \code{NULL}.}

\item{file_name}{Character. Name of the output file. Default is \code{"dtu_candidates.csv"}.}
}
\value{
A \code{data.frame} containing the identified DTU events.
}
\description{
Identifies Differential Transcript Usage (DTU) events by cross-referencing
global transcript-to-transcript mutual exclusivity (via COEX scores and p-values)
with cluster-specific Differential Expression Analysis (DEA).
It reports ALL cell populations where the transcripts show a significant relative switch.
}
