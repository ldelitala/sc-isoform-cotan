% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_coex.R
\name{prepare_to_coex}
\alias{prepare_to_coex}
\title{Prepare COTAN Object for COEX Calculation}
\usage{
prepare_to_coex(
  cotan_obj,
  threshold = 0.01,
  cores = 1L,
  max_iterations = 100L,
  chunk_size = 1024L,
  estimator_logs = FALSE,
  output_dir = NULL,
  file_name = "cotan_prepared.rds"
)
}
\arguments{
\item{cotan_obj}{A \code{COTAN} object.}

\item{threshold}{Numeric. Threshold for convergence in the solver. Default is \code{0.01}.}

\item{cores}{Integer. Number of cores to use for parallel execution. Default is \code{1L}.}

\item{max_iterations}{Integer. Maximum number of iterations for the solver. Default is \code{100L}.}

\item{chunk_size}{Integer. Size of chunks for parallel processing. Default is \code{1024L}.}

\item{estimator_logs}{Boolean. Flag to enable logs from solver estimator. Default is FALSE.}

\item{output_dir}{Character. Optional directory to save the output object as an RDS file. Default is \code{NULL}.}

\item{file_name}{Character. Optional file name for saving the output object. Default is \code{"cotan_prepared.rds"}.}
}
\value{
An updated \code{COTAN} object ready for COEX calculation.
}
\description{
Estimates parameters (lambda, nu, dispersion) needed for COEX calculation.
}
