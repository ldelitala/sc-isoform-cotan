% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{update_cell_clusterization}
\alias{update_cell_clusterization}
\title{Update or Add a Clusterization in a COTAN Object}
\usage{
update_cell_clusterization(
  cotan_obj,
  cluster_data,
  cluster_name,
  default_value = "Unknown",
  strip_suffix = TRUE,
  override = FALSE
)
}
\arguments{
\item{cotan_obj}{A \code{COTAN} object.}

\item{cluster_data}{A named vector. Names must be cell barcodes, and values are the cluster labels.}

\item{cluster_name}{Character. Name of the clusterization to add.}

\item{default_value}{The value assigned to cells not present in \code{cluster_data} (used only on first creation).
Defaults to \code{"Unknown"}.}

\item{strip_suffix}{Boolean. If TRUE, removes suffixes from barcodes (e.g., '-1') for safer matching.
Default is TRUE.}

\item{override}{Boolean. If TRUE, allows updating an existing clusterization. Default is FALSE.}
}
\value{
A \code{COTAN} object with the updated clusterization.
}
\description{
Modifies a COTAN object by adding or updating a specific cell clusterization.
It accepts a named vector, separating the data parsing logic from the
object manipulation. By setting \code{override = TRUE}, you can call this
function iteratively to append new cell types to the same clusterization.
Note: Updating an existing clusterization will automatically reset its
associated COEX data.frame (DEA), as the underlying cell groups have changed.
}
