% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_gdi_threshold_simulation.R
\name{run_gdi_mixture_simulation}
\alias{run_gdi_mixture_simulation}
\title{Run GDI Mixture Simulation}
\usage{
run_gdi_mixture_simulation(
  cotan_obj,
  cell_type_a,
  cell_type_b,
  proportions = c(0, 0.05, 0.1, 0.2, 0.4, 0.8),
  num_cells = 1000L,
  cond_name = "cell_type",
  seed = 42,
  cores = 1L,
  optimize_for_speed = TRUE,
  device_str = "cuda"
)
}
\arguments{
\item{cotan_obj}{A \code{COTAN} object.}

\item{cell_type_a}{Character. First cell type name (e.g. "T-cell").}

\item{cell_type_b}{Character. Second cell type name (e.g. "B-cell").}

\item{proportions}{Numeric vector. The mixing proportions of \code{cell_type_a} to test. Default: \code{c(0, 0.05, 0.1, 0.2, 0.4, 0.8)}.}

\item{num_cells}{Integer. Total number of cells to sample for each mixture. Default is \code{1000L}.}

\item{cond_name}{Character. Name of the cell type metadata condition. Default is \code{"cell_type"}.}

\item{seed}{Integer. Random seed for reproducibility. Default is \code{42}.}

\item{cores}{Integer. Number of cores for bisection dispersion estimation and GDI. Default is \code{1L}.}

\item{optimize_for_speed}{Logical. Use torch acceleration for COEX. Default is \code{TRUE}.}

\item{device_str}{Character. Device for torch calculations ("cpu", "cuda"). Default is \code{"cuda"}.}
}
\value{
A list of GDI vectors/data.frames for each tested proportion.
}
\description{
Simulates mixtures of two cell types at various proportions and runs the COTAN pipeline
to calculate the GDI for each mixture.
}
